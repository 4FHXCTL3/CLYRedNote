# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Increased memory allocation for better performance
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError

# Build Performance Optimizations
# Enable parallel execution for multi-module projects
org.gradle.parallel=true

# Enable Gradle daemon for faster builds
org.gradle.daemon=true

# Enable build caching for faster incremental builds
org.gradle.caching=true

# Enable configuration caching for faster configuration phase
org.gradle.configuration-cache=true

# Enable file system watching for better incremental builds
org.gradle.vfs.watch=true

# Enable modular compilation for better incremental compilation
org.gradle.kotlin.dsl.allWarningsAsErrors=false

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Android build optimizations
# Enable R8 full mode for better optimization
android.enableR8.fullMode=true

# Enable incremental compilation for faster Kotlin builds
kotlin.incremental=true

# Enable incremental compilation for Java as well
kotlin.incremental.java=true

# Use parallel GC for the Kotlin compiler
kotlin.compiler.execution.strategy=in-process

# Suppress unsupported compileSdk warning to reduce noise
android.suppressUnsupportedCompileSdk=36

# Enable non-transitive R classes to reduce APK size and build time
android.nonTransitiveRClass=true

# Enable new resource processing for faster builds
android.enableNewResourceProcessing=true